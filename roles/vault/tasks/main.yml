- name:
  template:
    src: copia.txt.j2
    dest: /tmp/copia.txt

- name:
  debug:
     msg: El host es "{{ vault.host }}" y el usuario es "{{ vault.username }}"  con passwwd "{{ vault.passwd }}"
  with_dict: "{{ vault }}"   